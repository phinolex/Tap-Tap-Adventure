define([], function() {
   //handle large arrays of data externally, so it preserves space.
    var GameData = Class.extend({
        init: function() {
             // sprites
               
            
        }  
        
    });
    return GameData;
});
