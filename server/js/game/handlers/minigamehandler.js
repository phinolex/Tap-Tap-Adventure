var cls = require('../lib/class');

module.exports = MinigameHandler = cls.Class.extend({
    init: function(id, name) {
        this.id = id;
        this.name = name;
    },

    getMinigameById: function() {

    }
});