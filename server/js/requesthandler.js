var cls = require("./lib/class");

module.exports = RequestHandler = cls.Class.extend({
   
   init: function(player, targetPlayer) {
       this.player = player;
       this.targetPlayer = targetPlayer;
       
   },
   
   
    
    
});